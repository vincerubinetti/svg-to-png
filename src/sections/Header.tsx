const Header = () => (
  <header>
    <h1>
      <img
        src="logo.svg"
        style={{ height: "1.2em", aspectRatio: "1 / 1" }}
        alt=""
      />
      SVG to PNG
    </h1>
  </header>
);

export default Header;
